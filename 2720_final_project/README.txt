Token Buy: A Token Commerce Platform
Instructions to setup:

0. Install the necessary modules by typing "npm install"

1. Start mongodb first before running node. We use original database url in the template, eg “mongodb://localhost/csci2720”

2. Run "node initdb.js" to reset the data in the DB.

3. Run "node index.js" to start the server.
